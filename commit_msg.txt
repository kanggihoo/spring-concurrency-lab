feat: setup PostgreSQL monitoring with postgres-exporter

### Summary
1.  **PostgreSQL Configuration**: Added `01_monitoring.sql` to create a dedicated `prometheus` user and enabled the `pg_stat_statements` extension for query tracking.
2.  **PostgreSQL Tuning**: Configured `postgresql.conf` to load `pg_stat_statements` via `shared_preload_libraries` and track all statements.
3.  **Docker Compose Setup**: Integrated `postgres_exporter` container, linked it to PostgreSQL with `healthcheck` and `depends_on`, and resolved mount issues.
4.  **Prometheus Integration**: Configured `prometheus.yml` to continuously scrape the newly exposed DB metrics endpoints.
5.  **Documentation Update**: Reorganized project plan files and updated `01_monitring-setup-summary.md` with detailed troubleshooting and setup details.

### Details
For detailed setup guides, troubleshooting logs, and architecture details, please refer to:
- docs/01_2_PLAN-postgresql-monitoring.md
- docs/01_monitring-setup-summary.md
